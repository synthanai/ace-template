# agentic-cognitive-ecosystem: The Sovereign GitHub Template

**Status:** CANONICAL  
**Purpose:** The baseline operating system drivers fork on Day 2 (Movement 4).  
**Philosophy:** A repo is not a storage drive; it is a cognitive garage.

---

## The Directory Architecture

This is the exact folder structure of the `agentic-cognitive-ecosystem` template repository that drivers will fork. It explicitly delivers the **ARC (Anatomy of Execution)** as the primary value stream.

```text
agentic-cognitive-ecosystem/
├── README.md                      # The Glass Hood
├── identity.yaml                  # The Ignition (DEFAULT -> FIRE/AIR/WATER)
├── .agent/                        # The Invisible Engine Block (The 11 Systems)
│   ├── harness/                   # Core biological software loop
│   ├── skills/                    # Enterprise-grade AI capabilities
│   └── workflows/                 # The ARC Verbs (Steal, Sparkit, Forge, etc.)
├── 1-concepts/                    # User workspace: Ideas and thinking
├── 2-research/                    # User workspace: Captured external intelligence
├── 3-projects/                    # User workspace: Active execution and outputs
└── 4-operations/                  # System state: Spar debates, logs, queues
```

---

## 1. The 11-System Delivery (The Value Shock)

The USER's strategic pivot is fully operationalized here. We are not delivering a markdown folder of prompts. We are delivering an enterprise-grade, executable 11-system biological software harness.

When the driver forks this repo, the sheer terror and awe of seeing 11 complex Python/TS microservices inside the `.agent/harness/` folder instantly proves they bought a $10,000 Lamborghini for $49. The fact that the engine is hidden in an `.agent` folder is intentional: it provides a clean workspace in the root directory for their actual files, while the invisible engine runs in the background.

## 2. The Tuning Mechanism (identity.yaml)

To prevent technical paralysis, drivers are explicitly told **not** to touch the `.agent/` directory. The Harness is the Body; it executes.

Instead, they govern the **Intent** (The Soul). In Day 2, Movement 3, they open the `identity.yaml` file in the root directory. They change `element: DEFAULT` to their Day 1 tuning spec (e.g., `element: FIRE`). The 11 systems dynamically read this YAML and alter their execution rhythm—Outer Loop speeds up, Context Pruning gets aggressive, Prompts become sharper.

This delivers the visceral "G-force" of tuning a complex system without requiring them to write a single line of logic.

## 3. The PACE Pitch (The Butterfly Effect)

This structure makes the 12-week PACE build inevitable. The coach's pitch becomes: *"You now own the full 11-system organism. But right now, you only know how to turn the key. PACE is where we spend 12 weeks taking the engine block apart and tuning every single organ to your life."*

## 2. The README (The Website)

The `README.md` in this template is designed to be instantly deployable via Vercel/Netlify in Day 2, Movement 7. It serves as a public-facing dashboard. 

It contains:
- Name and Identity (Agentic Cognitive Engineer)
- Elemental Stance
- Active Protocols

## 3. The Physical-Digital Oscillation (M6)

During Movement 6, drivers draft their recovery protocol in the physical Logbook. They are then instructed to immediately commit it to `3-PROTOCOLS/recovery_protocol.md` to prove ownership of the system. 

---

```
Document Status: CANONICAL (v1.0)
Created: May 10, 2026
Programme: ACE
```
